Instructions:

- Keep title format as `*: {concise summary}`.
- Use one of these title prefixes based on scope:
  - `Docs: {concise summary}` for docs-only updates.
  - `{milestone id}: {concise summary}` for milestone implementation work (examples: `M8c: ...`, `M9h: ...`, `M16b: ...`).
  - Do not use milestone-id title prefixes for planning/roadmap/doc-only milestone updates; use `Docs:` for those commits.
  - `Bugfix: {concise summary}` for defect fixes.
  - `Feature: {concise summary}` for net-new feature work outside milestone framing.
  - `Refactor: {concise summary}` for internal code restructuring with no behavior change.
  - `Test: {concise summary}` for test-only additions/updates.
  - `Chore: {concise summary}` for tooling/maintenance updates.
- Keep the summary concise and outcome-focused.
- Use 1-8 bullet points max in the message body.
- Keep bullets high-level; avoid excessive implementation detail.
- Update only the `Commit Message` section for each new commit.
- Do not modify this `Instructions` section unless explicitly requested.


Commit Message:

M10i5: Browse the desktop library through the list query

- Drive the infinite-scrolling library grid from `POST /api/library/query` for filter, search, sort, and scroll.
- Patch a loaded tile when a favorite, blacklist, playback, or tag change cannot affect membership or order. Reload the loaded window, keeping scroll, when it can. A splice that stops halfway does not shrink that window. Holding the scrollbar does not request further pages while it is torn, and a deferred filter, sort, or reload is not appended onto the old tiles. A tile update while a browse query is open, including a further page that has not been applied yet, reads that query again.
- Keep the filtered count on the server total, including while tiles are selected. Apply thumbnail fields and reflow when an aspect changes, including on the unchanged tiles after an inserted or removed id. A panel resize reflows after the current grid update and keeps the viewport tile.
- Replace the grid from the start only when the filter, search, preset, or sort changes. Showing the panel again keeps the loaded window.
- Send scoped auto-tag scans with no client path list. Keep the full catalog snapshot for non-browse readers. Now-playing stats use a loaded tile when the snapshot does not have that file, and download the snapshot when the current file is in neither copy. A playback for any other file missing from both copies still refreshes global play totals. A tag event applies the rest of its items before that snapshot download.
- Cancel an in-progress manual or automatic library refresh on server shutdown without recording it as a failure or a completed refresh. A probe canceled by shutdown is not stored as a missing tool or a failed frame. A forced duration or loudness rescan stays pending if shutdown cancels that stage.
