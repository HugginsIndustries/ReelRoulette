Instructions:

- Keep title format as `*: {concise summary}`.
- Use one of these title prefixes based on scope:
  - `Docs: {concise summary}` for docs-only updates.
  - `{milestone id}: {concise summary}` for milestone implementation work (examples: `M8c: ...`, `M9h: ...`, `M16b: ...`).
  - Do not use milestone-id title prefixes for planning/roadmap/doc-only milestone updates; use `Docs:` for those commits.
  - `Bugfix: {concise summary}` for defect fixes.
  - `Feature: {concise summary}` for net-new feature work outside milestone framing.
  - `Refactor: {concise summary}` for internal code restructuring with no behavior change.
  - `Test: {concise summary}` for test-only additions/updates.
  - `Chore: {concise summary}` for tooling/maintenance updates.
- Keep the summary concise and outcome-focused.
- Use 1-8 bullet points max in the message body.
- Keep bullets high-level; avoid excessive implementation detail.
- Update only the `Commit Message` section for each new commit.
- Do not modify this `Instructions` section unless explicitly requested.


Commit Message:

M9c: Linux AppImage packaging and GitHub install script

- Add `package-serverapp-linux-appimage.sh`, `package-desktop-linux-appimage.sh`, and `tools/scripts/lib/appimage-helpers.sh` (portable tar in, `artifacts/packages/appimage/*.AppImage` out; `--help` / `--install` on `AppRun`).
- Add `install-linux-from-github.sh` (latest release via API; AppImage preferred, tar.gz fallback; user-local install, no sudo).
- Run AppImage scripts from `full-release.ps1` on Linux after portable packaging.
- `set-release-version.ps1`: desktop version, contract regen, and verify default on (`-No*` to skip). `full-release.ps1`: optional `-Version` (skip set-release when omitted); forward `-NoDocUpdates` / `-No*` to `set-release-version` when version set; README/dev-setup/domain-inventory updated.
- Update dev-setup, README (Quick Start + Packaging: Linux/Windows/General), architecture, domain-inventory, CONTEXT, testing checklist, and MILESTONES (complete M9c; tracker `Last milestone completed`).
