Instructions:

- Keep title format as `*: {concise summary}`.
- Use one of these title prefixes based on scope:
  - `Docs: {concise summary}` for docs-only updates.
  - `{milestone id}: {concise summary}` for milestone implementation work (examples: `M8c: ...`, `M9h: ...`, `M16b: ...`).
  - Do not use milestone-id title prefixes for planning/roadmap/doc-only milestone updates; use `Docs:` for those commits.
  - `Bugfix: {concise summary}` for defect fixes.
  - `Feature: {concise summary}` for net-new feature work outside milestone framing.
  - `Refactor: {concise summary}` for internal code restructuring with no behavior change.
  - `Test: {concise summary}` for test-only additions/updates.
  - `Chore: {concise summary}` for tooling/maintenance updates.
- Keep the summary concise and outcome-focused.
- Use 1-8 bullet points max in the message body.
- Keep bullets high-level; avoid excessive implementation detail.
- Update only the `Commit Message` section for each new commit.
- Do not modify this `Instructions` section unless explicitly requested.


Commit Message:

M9f: WebUI polish — refresh parity, theme, controls, tag reorder

- Align WebUI refresh status with desktop completion summary and running-stage formatting; SSE path uses `buildRefreshStatusMessage` + `coerceRefreshSnapshot` from `refreshStatusProjection.ts`.
- Tag editor: category move up/down sets `tagEditorCategoryOrderDirty` so Apply enables and existing upsert-category apply persists order.
- Overlay: mute in bottom transport row (video-only), edit-tags top-right left of favorite; control-only drop shadows (no full media scrim).
- System light/dark via `prefers-color-scheme` (`theme-light` / `theme-dark`) and CSS variables for shell + tag editor surfaces.
- Milestone/docs: `MILESTONES.md` completion entry, `CHANGELOG.md` `[Unreleased]`, `CONTEXT.md`, `docs/checklists/testing-checklist.md`.
- WebUI pairing chrome: themed `.pair-section`; sibling of title with flex-wrap (inline between title and preset on wide viewports, stacked under 600px); non-shrinking `#app > header`, no errant `flex-grow` on title/pair rows; under 600px token field flex-fills width with **Pair** on one line (`pair-section-label-hint`).
- WebUI pairing visibility: strip hidden by default; show on **401** from APIs (e.g. `fetchJson`, random play); hide after successful `/api/version` so auth-off or existing session does not require `pairToken` in runtime config.
