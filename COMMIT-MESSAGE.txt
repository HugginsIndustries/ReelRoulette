Instructions:

- Keep title format as `*: {concise summary}`.
- Use one of these title prefixes based on scope:
  - `Docs: {concise summary}` for docs-only updates.
  - `{milestone id}: {concise summary}` for milestone implementation work (examples: `M8c: ...`, `M9h: ...`, `M16b: ...`).
  - Do not use milestone-id title prefixes for planning/roadmap/doc-only milestone updates; use `Docs:` for those commits.
  - `Bugfix: {concise summary}` for defect fixes.
  - `Feature: {concise summary}` for net-new feature work outside milestone framing.
  - `Refactor: {concise summary}` for internal code restructuring with no behavior change.
  - `Test: {concise summary}` for test-only additions/updates.
  - `Chore: {concise summary}` for tooling/maintenance updates.
- Keep the summary concise and outcome-focused.
- Use 1-8 bullet points max in the message body.
- Keep bullets high-level; avoid excessive implementation detail.
- Update only the `Commit Message` section for each new commit.
- Do not modify this `Instructions` section unless explicitly requested.


Commit Message:

Docs: reshape post-M9a Linux milestones and add library migration track

- Expand Linux Packaging planning: self-contained `linux-x64` tarballs, stripped symbols, shell launch wrappers, native prereq documentation, artifact naming aligned with Windows releases, and author pre-land extract/launch checks; defer AppImage, GitHub install script, and menu registration to Linux Installation UX.
- Add Linux Installation UX: AppImage builds for server and Desktop, user-local install script from GitHub Releases, and automatic application-menu (`.desktop`) registration paths.
- Rework CI Linux Distribution Gates around a `package-linux.yml` peer to `package-windows.yml`, producing portable and AppImage artifacts and uploading Linux release assets on tag push.
- Extend Linux Documentation and Operator Runbook scope to cover AppImage and install-script workflows.
- Add Cross-Platform Library Migration: export/import zip with `export-manifest.json`, per-source remapping (including skip), optional thumbnails and backups, symmetric Windows/Linux moves without a data-model change.
- Renumber Linux Release Readiness and Sign-off and fold deferred verification from the new packaging and installation milestones (including AppImage and install-script matrix items).
