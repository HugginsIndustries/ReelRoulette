Instructions:

- Keep title format as `*: {concise summary}`.
- Use one of these title prefixes based on scope:
  - `Docs: {concise summary}` for docs-only updates.
  - `{milestone id}: {concise summary}` for milestone implementation work (examples: `M8c: ...`, `M9h: ...`, `M16b: ...`).
  - Do not use milestone-id title prefixes for planning/roadmap/doc-only milestone updates; use `Docs:` for those commits.
  - `Bugfix: {concise summary}` for defect fixes.
  - `Feature: {concise summary}` for net-new feature work outside milestone framing.
  - `Refactor: {concise summary}` for internal code restructuring with no behavior change.
  - `Test: {concise summary}` for test-only additions/updates.
  - `Chore: {concise summary}` for tooling/maintenance updates.
- Keep the summary concise and outcome-focused.
- Use 1-8 bullet points max in the message body.
- Keep bullets high-level; avoid excessive implementation detail.
- Update only the `Commit Message` section for each new commit.
- Do not modify this `Instructions` section unless explicitly requested.


Commit Message:

M8i: complete desktop UX/UI polish for duplicates, tag surfaces, and refresh summary

- add per-item duplicate-review thumbnails in the desktop duplicates dialog using core server thumbnail endpoint paths with explicit desktop bitmap loading.
- add per-group duplicate handling selection so users can skip groups (`Keep All`) or keep a specific item, plus a persisted desktop setting for default behavior (`Keep All` default, `Select Best` legacy).
- improve duplicate delete confirmation by showing selected groups/files totals and skipping DELETE confirmation when no groups are selected.
- add duplicate comparison metadata in dialog rows/dropdowns (tag counts + filename/plays/tags/favorite/blacklisted labels) for easier keep decisions.
- align desktop tag editor and filter `Tags` visuals with shared theme-compatible category/chip surfaces, white chip text/icons with WebUI-like text/icon shadows and state-specific inset shadows, and responsive wrapping while preserving filter-only control boundaries.
- improve desktop core refresh completion status with a consolidated `Core refresh complete | Source | Duration | Loudness | Thumbnails` line built from compact stage-derived tokens (for example `no changes`, `files N, all cached`, non-zero-only thumbnail counters).
- verify implementation with full solution build and test pass.
