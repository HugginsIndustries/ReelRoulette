Instructions:

- Keep title format as `*: {concise summary}`.
- Use one of these title prefixes based on scope:
  - `Docs: {concise summary}` for docs-only updates.
  - `{milestone id}: {concise summary}` for milestone implementation work (examples: `M8c: ...`, `M9h: ...`, `M16b: ...`).
  - Do not use milestone-id title prefixes for planning/roadmap/doc-only milestone updates; use `Docs:` for those commits.
  - `Bugfix: {concise summary}` for defect fixes.
  - `Feature: {concise summary}` for net-new feature work outside milestone framing.
  - `Refactor: {concise summary}` for internal code restructuring with no behavior change.
  - `Test: {concise summary}` for test-only additions/updates.
  - `Chore: {concise summary}` for tooling/maintenance updates.
- Keep the summary concise and outcome-focused.
- Use 1-8 bullet points max in the message body.
- Keep bullets high-level; avoid excessive implementation detail.
- Update only the `Commit Message` section for each new commit.
- Do not modify this `Instructions` section unless explicitly requested.


Commit Message:

Bugfix: WebUI PWA service worker and desktop LibVLC video embed for Avalonia 12

- Add `public/sw.js` (network-only `fetch`) and register from `main.ts` in secure contexts for Chromium installability with the existing manifest.
- Serve `sw.js` with `Cache-Control: no-store` from WebUI static hosting; require `dist/sw.js` in `verify-build-output.mjs`.
- Document installability and Android Chrome vs Firefox notes in `README.md`, `docs/dev-setup.md`, `CONTEXT.md`, `docs/architecture.md`, and `docs/domain-inventory.md`; fold service worker into `CHANGELOG.md` **0.11.0** WebUI PWA bullet.
- Branch docs: complete `docs/checklists/testing-checklist.md` regression pass metadata/checks and add release date to `CHANGELOG.md` **0.11.0** heading.
- Desktop: `LibVLC` with `--intf dummy` and `EnsureVideoViewMediaPlayerAttached()` (Loaded, `AttachedToVisualTree`, before video `Play`) so `VideoView` gets `Hwnd`/`XWindow` after Avalonia 12 native host timing; log fix under `CHANGELOG.md` **0.11.0** Fixed.
