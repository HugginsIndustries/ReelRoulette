Instructions:

- Keep title format as `*: {concise summary}`.
- Use one of these title prefixes based on scope:
  - `Docs: {concise summary}` for docs-only updates.
  - `{milestone id}: {concise summary}` for milestone implementation work (examples: `M8c: ...`, `M9h: ...`, `M16b: ...`).
  - Do not use milestone-id title prefixes for planning/roadmap/doc-only milestone updates; use `Docs:` for those commits.
  - `Bugfix: {concise summary}` for defect fixes.
  - `Feature: {concise summary}` for net-new feature work outside milestone framing.
  - `Refactor: {concise summary}` for internal code restructuring with no behavior change.
  - `Test: {concise summary}` for test-only additions/updates.
  - `Chore: {concise summary}` for tooling/maintenance updates.
- Keep the summary concise and outcome-focused.
- Use 1-8 bullet points max in the message body.
- Keep bullets high-level; avoid excessive implementation detail.
- Update only the `Commit Message` section for each new commit.
- Do not modify this `Instructions` section unless explicitly requested.


Commit Message:

M9g: Linux sign-off, XDG autostart content-root fix, Avalonia 12 + dependency refresh

- Complete **Linux Release Readiness and Sign-off**: move milestone to **Completed** in `MILESTONES.md` with CachyOS `linux-x64` command evidence (Release `dotnet build`/`test` + `EnableWindowsTargeting`, WebUI `npm run verify`, `verify-web-deploy.ps1`, Linux portable + AppImage packaging, `verify-linux-packaged-server-smoke.sh`, AppImage `--help`, `install-linux-local.sh` with isolated `HOME`).
- Update `docs/checklists/testing-checklist.md` metadata, packaging/tray rows, sign-off **PASS**, and explicit waivers for Windows-only installer/packaging checks and live `install-linux-from-github.sh` against a tag.
- `CHANGELOG.md` `[Unreleased]`, `CONTEXT.md`, `README.md`, `docs/architecture.md`, and `docs/domain-inventory.md`: Linux autostart / host behavior and this sign-off.
- **Bugfix:** `ReelRoulette.ServerApp` uses `WebApplication.CreateBuilder` with `ContentRootPath = AppContext.BaseDirectory`; `LinuxXdgStartupLaunchService` writes `reelroulette-server.desktop` with `Exec=`/`Path=` from a stable path (prefers **`APPIMAGE`** for AppImage so autostart is not pinned to `/tmp/.mount_*`) so login startup resolves `appsettings`/`wwwroot` and cwd matches portable `run-server.sh`.
- **Dependencies:** **Avalonia** **12.0.0** (ServerApp + DesktopApp); drop **Avalonia.Diagnostics** (no 12.x on NuGet; unused `WithDevTools()`); **Tmds.DBus.Protocol** **0.92.0** on ServerApp + DesktopApp (**NU1903** / **GHSA-xrw6-gwf8-vvr9**); **SkiaSharp** **3.119.2**, **LibVLCSharp.Avalonia** **3.9.6**, **VideoLAN.LibVLC.Windows** **3.0.23**, test SDK/xunit bumps—see **`CHANGELOG.md` `[Unreleased]`**.
- Avalonia 12 code follow-ups: **ToggleButton** `IsCheckedChanged`, clipboard **`ClipboardExtensions`**, **`MenuItemToggleType`**, **`TextBox.PlaceholderText`** (replaces **`Watermark`**), thumbnail scaling **`SKSamplingOptions`**.
- **Known issue:** **Desktop** client is not usable at runtime on Avalonia **12** until **LibVLCSharp** publishes **Avalonia 12**–compatible **`LibVLCSharp.Avalonia`** (current **3.9.6** targets older Avalonia APIs); waiting on upstream.
