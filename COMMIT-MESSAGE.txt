Instructions:

- Keep title format as `*: {concise summary}`.
- Use one of these title prefixes based on scope:
  - `Docs: {concise summary}` for docs-only updates.
  - `{milestone id}: {concise summary}` for milestone implementation work (examples: `M8c: ...`, `M9h: ...`, `M16b: ...`).
  - Do not use milestone-id title prefixes for planning/roadmap/doc-only milestone updates; use `Docs:` for those commits.
  - `Bugfix: {concise summary}` for defect fixes.
  - `Feature: {concise summary}` for net-new feature work outside milestone framing.
  - `Refactor: {concise summary}` for internal code restructuring with no behavior change.
  - `Test: {concise summary}` for test-only additions/updates.
  - `Chore: {concise summary}` for tooling/maintenance updates.
- Keep the summary concise and outcome-focused.
- Use 1-8 bullet points max in the message body.
- Keep bullets high-level; avoid excessive implementation detail.
- Update only the `Commit Message` section for each new commit.
- Do not modify this `Instructions` section unless explicitly requested.


Commit Message:

Bugfix: server library relativePath, cross-OS archive import remap, desktop portal storage paths, and Linux AppImage install layout

- Server `relativePath` now uses `Path.GetRelativePath` via `ReelRoulette.Core.Storage.LibraryRelativePath` (replacing `Uri.MakeRelativeUri`) in `RefreshPipelineService` and `LibraryOperationsService`, fixing spurious leading `..` when roots use trailing separators.
- Library import remapping repairs stored `relativePath` values that start with `..` by recomputing from the item `fullPath` and old source root using segment-based cross-platform logic (Windows drive and slash normalization so Linux imports of Windows exports work); `CombineRootAndRelative` uses `NormalizeRelativeForDestinationRoot` for opposite-separator and stray `X:` relatives on Unix.
- Added `LibraryRelativePathTests` (including Windows/Linux synthetic paths) and `LibraryArchiveMigration` remap/`CombineRootAndRelative` coverage.
- Add `tools/scripts/install-linux-local.sh` and align `install-linux-from-github.sh` AppImage installs to the same `~/.local/share/ReelRoulette` stable filenames (`REELROULETTE_LOCAL_APPIMAGE_DIR`); document in README, `docs/dev-setup.md`, `CONTEXT.md`, `docs/domain-inventory.md`, CHANGELOG, and testing checklist.
- Desktop storage pickers use `StorageProviderExtensions.TryGetLocalPath` with a shared `StoragePickerPath` helper so library import remap, zip import/export, and **Import Folder** get real paths when portal pickers leave `Path.LocalPath` empty; remap dialog uses `ClearValue` for normal path text color instead of `Foreground = null`.
