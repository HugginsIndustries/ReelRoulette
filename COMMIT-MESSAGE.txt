Instructions:

- Keep title format as `*: {concise summary}`.
- Use one of these title prefixes based on scope:
  - `Docs: {concise summary}` for docs-only updates.
  - `{milestone id}: {concise summary}` for milestone implementation work (examples: `M8c: ...`, `M9h: ...`, `M16b: ...`).
  - Do not use milestone-id title prefixes for planning/roadmap/doc-only milestone updates; use `Docs:` for those commits.
  - `Bugfix: {concise summary}` for defect fixes.
  - `Feature: {concise summary}` for net-new feature work outside milestone framing.
  - `Refactor: {concise summary}` for internal code restructuring with no behavior change.
  - `Test: {concise summary}` for test-only additions/updates.
  - `Chore: {concise summary}` for tooling/maintenance updates.
- Keep the summary concise and outcome-focused.
- Use 1-8 bullet points max in the message body.
- Keep bullets high-level; avoid excessive implementation detail.
- Update only the `Commit Message` section for each new commit.
- Do not modify this `Instructions` section unless explicitly requested.


Commit Message:

M10i1: Add a SQLite library catalog store and JSON migration

- Migrate library.json into library.db in the roaming config directory, and leave the running server reading and writing library.json.
- Publish the database only after it is committed and synced through a writable handle, sync the directory after that publish (directory-list access on Windows), and rename library.json only after the directory sync succeeds.
- Quarantine a partial, unversioned, corrupt, or non-database file, including one whose header opens but whose rows are corrupt, without consuming library.json or the migration-time snapshot. Leave a locked or busy library.db in place and fail that open within about a second.
- Do not publish library.db when library.json.migrated is already present, and name that snapshot only when the file exists.
- Keep a missing fingerprintStatus null so a legacy SHA-256 fingerprint can still count as ready.
- Match the server loader: skip a source that lacks an id or root path, skip an item with no full path, force the uncategorized category name and sort order (and add it when missing), and keep the last tag when names collide case-insensitively.
- Disable pooling on catalog test connections so Windows can rename and delete library.db.
