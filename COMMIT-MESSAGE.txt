Instructions:

- Keep title format as `*: {concise summary}`.
- Use one of these title prefixes based on scope:
  - `Docs: {concise summary}` for docs-only updates.
  - `{milestone id}: {concise summary}` for milestone implementation work (examples: `M8c: ...`, `M9h: ...`, `M16b: ...`).
  - Do not use milestone-id title prefixes for planning/roadmap/doc-only milestone updates; use `Docs:` for those commits.
  - `Bugfix: {concise summary}` for defect fixes.
  - `Feature: {concise summary}` for net-new feature work outside milestone framing.
  - `Refactor: {concise summary}` for internal code restructuring with no behavior change.
  - `Test: {concise summary}` for test-only additions/updates.
  - `Chore: {concise summary}` for tooling/maintenance updates.
- Keep the summary concise and outcome-focused.
- Use 1-8 bullet points max in the message body.
- Keep bullets high-level; avoid excessive implementation detail.
- Update only the `Commit Message` section for each new commit.
- Do not modify this `Instructions` section unless explicitly requested.


Commit Message:

M9a: establish Linux runtime baseline (server + desktop)

- Switch ServerApp tray host to a cross-platform Avalonia tray with headless fallback and Linux XDG autostart support.
- Upgrade repo to .NET 10 and align CI with `net10.0`.
- Rename the legacy Windows client to the cross-platform Desktop client (`src/clients/windows` -> `src/clients/desktop`) and align the on-disk folder with the `.csproj` (`ReelRoulette.DesktopApp/`) across solution, tests, scripts, and current docs.
- Harden refresh pipeline + desktop projection sync to tolerate legacy enum serialization shapes.
- Hydrate desktop grid thumbnails after refresh completion and as the visible grid window changes (no full library rebuild).
- Bump Avalonia packages to 11.3.12 (desktop + ServerApp).
- Add server `fingerprintScan` refresh stage plus persisted `fingerprintScanMaxDegreeOfParallelism` refresh setting (desktop Settings) and desktop completion-line Fingerprint summary.
- Consolidate `tools/scripts` to cross-platform `.ps1` only: remove `.sh` counterparts; portable `Join-Path`; Windows-only `Start-Process` options guarded; leading `#!/usr/bin/env pwsh` plus Git `+x` on each script for direct execution on Unix; README/dev-setup prerequisites for PowerShell Core (`pwsh`, including Arch-based distros via AUR `powershell-bin` with `paru`/`yay`); contributor docs use POSIX repo-relative paths (`./src/...`) and forward slashes in Windows `%...%` examples.
