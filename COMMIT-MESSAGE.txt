Instructions:

- Keep title format as `*: {concise summary}`.
- Use one of these title prefixes based on scope:
  - `Docs: {concise summary}` for docs-only updates.
  - `{milestone id}: {concise summary}` for milestone implementation work (examples: `M8c: ...`, `M9h: ...`, `M16b: ...`).
  - Do not use milestone-id title prefixes for planning/roadmap/doc-only milestone updates; use `Docs:` for those commits.
  - `Bugfix: {concise summary}` for defect fixes.
  - `Feature: {concise summary}` for net-new feature work outside milestone framing.
  - `Refactor: {concise summary}` for internal code restructuring with no behavior change.
  - `Test: {concise summary}` for test-only additions/updates.
  - `Chore: {concise summary}` for tooling/maintenance updates.
- Keep the summary concise and outcome-focused.
- Use 1-8 bullet points max in the message body.
- Keep bullets high-level; avoid excessive implementation detail.
- Update only the `Commit Message` section for each new commit.
- Do not modify this `Instructions` section unless explicitly requested.


Commit Message:

Refactor: Thin desktop media/refresh; unified Windows native deps

- Desktop: remove dead local ffprobe duration path; trim NativeBinaryHelper to LibVLC only; library duration / ffprobe refresh stay server-authoritative.
- Desktop: remove orphaned client auto-refresh timer and pointer idle tracking; drop unused ParseLoudnessFromFFmpegOutput; keep settings → core sync for server refresh policy.
- Desktop Settings: remove legacy auto-refresh “only when idle” / idle-threshold state (server-owned scheduling).
- Add `tools/scripts/fetch-native-deps.ps1` (gyan.dev FFmpeg ZIP + LibVLC from NuGet cache or VideoLAN mirror; `.versions.json`, SHA-256, `-Force`); gitignore `runtimes/`, remove tracked `libvlc` tree, add `VideoLAN.LibVLC.Windows` to the desktop project.
- Windows packaging invokes fetch when `runtimes/win-x64/native/` is incomplete; stage ffmpeg/ffprobe into server publish and LibVLC into desktop publish (no Chocolatey).
- Align README, dev-setup, CONTEXT, domain-inventory, testing-checklist, and CHANGELOG [Unreleased].
