Instructions:

- Keep title format as `*: {concise summary}`.
- Use one of these title prefixes based on scope:
  - `Docs: {concise summary}` for docs-only updates.
  - `{milestone id}: {concise summary}` for milestone implementation work (examples: `M8c: ...`, `M9h: ...`, `M16b: ...`).
  - Do not use milestone-id title prefixes for planning/roadmap/doc-only milestone updates; use `Docs:` for those commits.
  - `Bugfix: {concise summary}` for defect fixes.
  - `Feature: {concise summary}` for net-new feature work outside milestone framing.
  - `Refactor: {concise summary}` for internal code restructuring with no behavior change.
  - `Test: {concise summary}` for test-only additions/updates.
  - `Chore: {concise summary}` for tooling/maintenance updates.
- Keep the summary concise and outcome-focused.
- Use 1-8 bullet points max in the message body.
- Keep bullets high-level; avoid excessive implementation detail.
- Update only the `Commit Message` section for each new commit.
- Do not modify this `Instructions` section unless explicitly requested.


Commit Message:

M9e: Desktop-only library zip migration

- Remove the in-repo server HTTP migration path: `POST /api/library/export` and `POST /api/library/import`, `LibraryMigrationService`, Core `LibraryMigration` (+ tests), `api.library.migration`, and the matching OpenAPI/WebUI contract surface.
- Reimplement library zip export/import as desktop-local disk I/O in new `ReelRoulette.LibraryArchive` (`net10.0`), referenced from the Desktop app; drop the corresponding API client methods.
- Wire **Library → Export Library…** / **Import Library…** to that path (export warning when core may be running; import server-stopped acknowledgment and **Import to disk**; overwrite confirm; local `desktop-settings.json`; resync when core is reachable), with `ReelRoulette.DesktopApp.Tests` coverage.
- MainWindow: indeterminate status progress + suppressed status churn + Library menu disable + wait cursor during export/import; explicit completion status strings.
- Sync CONTEXT, api, domain-inventory, dev-setup, testing-checklist, MILESTONES evidence, and `CHANGELOG.md` `[Unreleased]`; regenerate WebUI `openapi.generated.ts`.
